import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Example/Helpers" />

# Helpers

> Apply helper classes to almost any element, in order to alter their style

```tsx
const Block: React.FC<unknown> = ({ children, ...props }) => (
	<div {...props}>{children}</div>
);
const WithHelper = withHelpersModifiers(Block);
```

## Flexbox <small>[Bulma reference]()</small>

## Spacing <small>[Bulma reference]()</small>

#### Example:

```tsx
<WithHelper spacing={["pl-1", "mb-5"]} />
```

## Visibility <small>[Bulma reference]()</small>

---

### isInvisible

#### Property: `isInvisible`

#### Example:

```tsx
<WithHelper isInvisible />
```

### isSrOnly

#### Property: `isSrOnly`

#### Example:

```tsx
<WithHelper isSrOnly />
```
