import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Example/Helpers" />

# Helpers

> Apply helper classes to almost any element, in order to alter their style

```tsx
const Block: React.FC<unknown> = ({ children, ...props }) => (
	<div {...props}>{children}</div>
);
const WithHelper = withHelpersModifiers(Block);
```

- [Color](/example-helpers-color--page)
- [Flexbox](/example-helpers-flexbox--page)
- [Spacing](/example-helpers-spacing--page)
- [Visibility](/example-helpers-visibility--page)
- [Typography](/example-helpers-typography--page)
- [Other](/example-helpers-other--page)
